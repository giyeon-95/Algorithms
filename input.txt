1) 객체 하나를 생성해서 500까지 value 값들을 저장한 다음에 value에 K값이 있는 key 를 찾아서 return ?f



    if(sIndex + K < dayArr.length -1) {
        answer = dayArr[sIndex + K]; 
        return answer;
    }else {
        //만약 K값이 5다 ? 한바퀴 더 돌아야 함.

    }
